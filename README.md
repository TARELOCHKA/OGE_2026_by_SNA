# 📝 Система автоматической проверки сочинений ОГЭ

Веб-приложение для автоматической оценки сочинений ОГЭ по русскому языку с использованием GigaChat API. Система оценивает сочинения по четырем критериям (K1-K4) и предоставляет подробные объяснения для каждого балла.

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-3.0-green.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## ✨ Возможности

- 🎯 **Проверка одного сочинения** — детальный анализ с пояснениями по каждому критерию
- 📊 **Массовая проверка** — одновременная проверка нескольких сочинений через удобный UI
- 🎨 **Современный веб-интерфейс** — темный дизайн с интуитивной навигацией
- 🔍 **Фильтрация и поиск** — удобный выбор сочинений по типу и ID
- 📈 **Статистика** — автоматический подсчет средних баллов и общей суммы
- 🔄 **Retry и восстановление** — автоматическое исправление ошибок API и JSON
- 🚀 **Готово к деплою** — конфигурация для Amvera и других платформ

## 🚀 Быстрый старт

### Требования

- Python 3.10+ (рекомендуется 3.11 или 3.12)
- Токен GigaChat API
- Git (для клонирования репозитория)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/yourusername/oge-essay-scoring.git
   cd oge-essay-scoring
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   # Windows
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1

   # Linux/Mac
   python -m venv .venv
   source .venv/bin/activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Подготовьте данные:**
   ```bash
   python scripts/prepare_inputs.py
   ```

5. **Создайте файл `.env` в корне проекта:**
   ```env
   GIGACHAT_TOKEN=ваш_токен_здесь
   MAX_BATCH_SIZE=30
   REQUEST_TIMEOUT_SEC=60
   ```

6. **Запустите приложение:**
   ```bash
   python run.py
   ```

7. **Откройте в браузере:**
   - **Главная страница:** http://localhost:8080/ui
   - **Health check:** http://localhost:8080/health

## 📖 Использование

### Веб-интерфейс

1. **Проверка одного сочинения:**
   - Откройте вкладку "Одно сочинение"
   - Введите ID сочинения и нажмите "Загрузить" (или введите данные вручную)
   - Нажмите "Оценить"
   - Просмотрите детальные результаты с пояснениями по каждому критерию

2. **Проверка нескольких сочинений:**
   - Откройте вкладку "Несколько сочинений"
   - Используйте фильтры для поиска нужных сочинений
   - Выберите сочинения чекбоксами
   - Нажмите "Оценить выбранные"
   - Просмотрите таблицу результатов и детальные пояснения для каждого сочинения

3. **Batch режим (JSON):**
   - Откройте вкладку "JSON (batch)"
   - Вставьте JSON с массивом сочинений
   - Нажмите "Оценить пачкой"

### API эндпоинты

#### `GET /health`
Проверка работоспособности сервиса.

**Ответ:**
```json
{
  "status": "ok"
}
```

#### `POST /score_one`
Оценка одного сочинения.

**Тело запроса:**
```json
{
  "essay_id": "1",
  "essay_type": 2,
  "task_text": "Текст задания...",
  "reference_text_essay": "Эталонный текст...",
  "essay_text": "Текст сочинения ученика..."
}
```

**Ответ:**
```json
{
  "essay_id": "1",
  "K1": 1,
  "K1_explanation": "Позиция ученика ясно выражена...",
  "K2": 3,
  "K2_explanation": "Найдено два примера из текста...",
  "K3": 2,
  "K3_explanation": "Рассуждение логично выстроено...",
  "K4": 1,
  "K4_explanation": "Есть вступление, основная часть и вывод.",
  "meta": {
    "model": "GigaChat",
    "prompt_version": "v1.1",
    "attempt": 0,
    "essay_type": 2,
    "json_repair": false
  }
}
```

#### `POST /score_batch`
Оценка нескольких сочинений.

**Тело запроса:**
```json
{
  "items": [
    {
      "essay_id": "1",
      "essay_type": 2,
      "task_text": "...",
      "reference_text_essay": "...",
      "essay_text": "..."
    },
    {
      "essay_id": "2",
      "essay_type": 3,
      "task_text": "...",
      "reference_text_essay": "...",
      "essay_text": "..."
    }
  ]
}
```

**Ответ:**
```json
{
  "results": [...],
  "errors": [...]
}
```

#### `GET /api/essays`
Получить список всех доступных сочинений.

**Ответ:**
```json
{
  "essays": [
    {
      "essay_id": "1",
      "essay_type": 2,
      "task_text": "...",
      "essay_preview": "...",
      "essay_length": 250
    }
  ]
}
```

## 🛠️ Конфигурация

Все настройки можно изменить через переменные окружения в файле `.env`:

```env
# GigaChat API
GIGACHAT_TOKEN=ваш_токен
GIGACHAT_MODEL=GigaChat
TEMPERATURE=0.0
PROMPT_VERSION=v1.1
MAX_TOKENS=900

# Настройки безопасности
MAX_BATCH_SIZE=30
REQUEST_TIMEOUT_SEC=60
```

## 📁 Структура проекта

```
oge-essay-scoring/
├── app/                      # Flask приложение
│   ├── __init__.py          # Инициализация приложения
│   ├── routes.py            # API и UI эндпоинты
│   ├── scoring.py           # Логика оценки сочинений
│   ├── gigachat_client.py   # Клиент для GigaChat API
│   ├── data_store.py        # Работа с данными
│   ├── schemas.py           # Валидация данных
│   ├── prompting.py         # Промпты для LLM
│   ├── json_utils.py        # Утилиты для работы с JSON
│   ├── repair.py            # Восстановление поврежденного JSON
│   ├── templates/           # HTML шаблоны
│   │   └── app.html         # Главная страница UI
│   └── static/              # Статические файлы
├── data/                    # Данные
│   ├── essays.csv          # Исходные сочинения
│   ├── reference_text*.csv # Эталонные тексты
│   └── inputs_for_scoring.csv # Подготовленные данные
├── scripts/                 # Утилиты
│   ├── prepare_inputs.py   # Подготовка данных
│   ├── make_submission.py  # Генерация submission.csv
│   └── ...
├── wsgi.py                 # Точка входа для WSGI сервера
├── Procfile                # Конфигурация для деплоя
├── requirements.txt        # Зависимости Python
└── README.md              # Документация
```

## 🚀 Деплой

### Деплой на Amvera

Подробная инструкция по деплою доступна в [DEPLOY.md](DEPLOY.md).

**Кратко:**
1. Подключите Git репозиторий в Amvera
2. Установите переменные окружения
3. Amvera автоматически определит `wsgi.py` как точку входа
4. Приложение будет доступно по предоставленному URL

### Деплой на другие платформы

Проект готов к деплою на любую платформу, поддерживающую Python:
- **Heroku** — используйте `Procfile`
- **Railway** — автоматическое определение Python
- **Render** — используйте `Procfile`
- **DigitalOcean App Platform** — укажите `wsgi:app`

## 🔧 Разработка

### Запуск в режиме разработки

```bash
python run.py
```

Приложение запустится с автоперезагрузкой при изменении кода.

### Тестирование

Для тестирования API можно использовать:

```bash
# Health check
curl http://localhost:8080/health

# Проверка одного сочинения
curl -X POST http://localhost:8080/score_one \
  -H "Content-Type: application/json" \
  -d '{"essay_id":"1","essay_type":2,"task_text":"...","reference_text_essay":"...","essay_text":"..."}'
```

Или используйте веб-интерфейс: http://localhost:8080/ui

## 🐛 Решение проблем

### Ошибка "GIGACHAT_TOKEN is not set"
Убедитесь, что файл `.env` существует в корне проекта и содержит `GIGACHAT_TOKEN`.

### Ошибка "File not found: data/inputs_for_scoring.csv"
Запустите скрипт подготовки данных:
```bash
python scripts/prepare_inputs.py
```

### Ошибки парсинга JSON от модели
Система автоматически пытается восстановить поврежденный JSON. Если проблема сохраняется:
- Проверьте, что `TEMPERATURE=0.0` в `.env`
- Убедитесь, что токен GigaChat валиден

## 📝 Критерии оценки

Система оценивает сочинения по четырем критериям:

- **K1** (0-1 балл) — Формулировка проблемы и позиция автора
- **K2** (0-3 балла) — Комментарий к сформулированной проблеме с примерами
- **K3** (0-2 балла) — Отражение позиции автора и собственное мнение
- **K4** (0-1 балл) — Смысловая целостность и композиционная стройность

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👥 Авторы

- **Ваше имя** — [GitHub](https://github.com/yourusername)

## 🙏 Благодарности

- GigaChat API за предоставление модели для оценки
- Flask за отличный веб-фреймворк
- Всем контрибьюторам проекта

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте [Issue](https://github.com/yourusername/oge-essay-scoring/issues).

---

⭐ Если проект был полезен, поставьте звезду!
