# 🚀 Быстрый запуск проекта

## Шаг 1: Активация виртуального окружения

Если у вас еще нет виртуального окружения, создайте его:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

Если виртуальное окружение уже создано, просто активируйте его:

```powershell
.\.venv\Scripts\Activate.ps1
```

## Шаг 2: Установка зависимостей

```powershell
python -m pip install -r requirements.txt
```

## Шаг 3: Подготовка данных (если нужно)

Если файл `data/inputs_for_scoring.csv` отсутствует, выполните:

```powershell
python scripts/prepare_inputs.py
```

## Шаг 4: Создание файла .env (опционально)

Для работы с GigaChat создайте файл `.env` в корне проекта:

```env
GIGACHAT_TOKEN=ваш_токен_здесь
MAX_BATCH_SIZE=30
REQUEST_TIMEOUT_SEC=60
```

> ⚠️ Файл `.env` уже должен быть в `.gitignore`

## Шаг 5: Запуск сервера

### Вариант 1: Использовать run.py (рекомендуется)

```powershell
python run.py
```

### Вариант 2: Запустить напрямую

```powershell
python -c "from app import create_app; create_app().run(host='0.0.0.0', port=8080, debug=True)"
```

### Вариант 3: Использовать Flask CLI (если установлен)

```powershell
flask run --host=0.0.0.0 --port=8080
```

## 📱 Открыть в браузере

После запуска сервера откройте:

- **Главная страница UI**: http://localhost:8080/ui
- **Health check**: http://localhost:8080/health

## 🎯 Использование

1. **Одно сочинение**: Вкладка "Одно сочинение" - введите данные и нажмите "Оценить"
2. **Несколько сочинений**: Вкладка "Несколько сочинений" - выберите нужные чекбоксами и нажмите "Оценить выбранные"
3. **Batch (JSON)**: Вкладка "JSON (batch)" - вставьте JSON с массивом сочинений

## 🛑 Остановка сервера

Нажмите `Ctrl+C` в терминале, где запущен сервер.

## ❓ Проблемы?

- Убедитесь, что порт 8080 свободен
- Проверьте, что виртуальное окружение активировано
- Убедитесь, что установлены все зависимости из `requirements.txt`
